server {
  listen 80;
  location / { return 200 'cofiswarm-ui stub — wire cofiswarm-ui build in Sprint 14\n'; add_header Content-Type text/plain; }
  location /api/ { proxy_pass http://host.docker.internal:3002; }
}
