# Deprecated stub — use cofiswarm-ui Docker image (compose/stack.yml).
server {
  listen 80;
  location / { return 200 'cofiswarm-ui stub — run make ui-build && make up\n'; add_header Content-Type text/plain; }
  location /api/ { proxy_pass http://host.docker.internal:8010; }
}
